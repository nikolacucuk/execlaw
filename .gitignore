# Generated by Cargo
# will have compiled files and executables
debug
target

# Python bytecode caches — produced when running
# plugins/python-sandbox/smoke_execute.py from the host. Cached
# .pyc files are not stable across interpreter versions.
__pycache__/
*.pyc

# These are backup files generated by rustfmt
**/*.rs.bk

# MSVC Windows builds of rustc generate these, which store debugging information
*.pdb

# Generated by cargo mutants
# Contains mutation testing data
**/mutants.out*/

# RustRover
#  JetBrains specific template is maintained in a separate JetBrains.gitignore that can
#  be found at https://github.com/github/gitignore/blob/main/Global/JetBrains.gitignore
#  and can be added to the global gitignore or merged into this file.  For a more nuclear
#  option (not recommended) you can uncomment the following to ignore the entire idea folder.
#.idea/

# Node / SPA build artifacts.
node_modules/
web/dist/

# Built plugin install ZIPs (generated; rebuild with the
# zip-plugin commands in CONTRIBUTING).
/dist/

# Claude Code local session state — user-specific, never committed.
.claude/

# Editor scratch.
*.swp
.DS_Store
.git-commit-msg

# Test-leaked blob store. Any test that constructs a default
# `builtin_artifacts_root_path()` without an env override and runs
# from the server crate's cwd writes here — should use a TempDir
# instead. Tracked here as a safety net so the debris never gets
# committed.
crates/server/blobs/
/graphify-out
/target-verify/release/.fingerprint
/target-verify/release/build
/target-verify
/target-verify-2
/target-verify-3
/target-verify-4
/web/target-verify-3
/web/target-verify-4
/target-verify-7
