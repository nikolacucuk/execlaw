{
    "type": "object",
    "properties": {
        "to": {
            "type": "string",
            "description": "Direct-chat recipient as an E.164 phone number or WhatsApp JID, or a group JID ending in @g.us."
        },
        "limit": {
            "type": "integer",
            "description": "Maximum retained messages to return, newest first.",
            "default": 20,
            "minimum": 1,
            "maximum": 200
        },
        "refresh": {
            "type": "boolean",
            "description": "Request a WhatsApp history sync before reading. When true, wait before calling again with false.",
            "default": true
        }
    },
    "required": ["to"],
    "additionalProperties": false
}