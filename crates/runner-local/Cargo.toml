[package]
name = "execlaw-runner-local"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "In-tree runner. Talks to any OpenAI-compatible local LLM backend. No cloud SDKs."

[dependencies]
execlaw-core = { path = "../core" }
execlaw-inference-api = { path = "../inference-api" }
execlaw-context-window = { path = "../context-window" }
execlaw-session = { path = "../session" }
tokio.workspace = true
serde.workspace = true
serde_json.workspace = true
tracing.workspace = true
anyhow.workspace = true
thiserror.workspace = true
async-trait.workspace = true
chrono.workspace = true

# dev-dependencies were Criterion-only for the legacy thread_tool /
# memory_tool benchmarks; both modules moved to
# `execlaw_core::builtin_tools` and are now exercised by the
# `plugin_host_hot_paths` Criterion harness.
